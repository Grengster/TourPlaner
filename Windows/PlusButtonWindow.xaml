<Window x:Class="TourPlaner.PlusButtonWindow" x:Name="AddWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:TourPlaner"
        mc:Ignorable="d"
        Title="PlusButtonWindow" Height="450" Width="500" MinHeight="450" MinWidth="500">
    <Window.DataContext>
        <vm:AddTourViewModel />
    </Window.DataContext>
    <Grid Margin="40,30,40,30">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Label FontSize="30" Margin="40,30,40,30" Grid.ColumnSpan="2" Grid.RowSpan="2">Type your tourname:</Label>
        <TextBox Grid.Row="1" Margin="40,10,40,15" Grid.ColumnSpan="2" Text="{Binding Input, UpdateSourceTrigger=PropertyChanged}"/>
        <Label Grid.Row="1" HorizontalAlignment="Left" Content="{Binding Input}" VerticalAlignment="Top" FontSize="14" RenderTransformOrigin="0.369,0.643" Grid.ColumnSpan="2" Margin="166,152,0,0"/>
        <Button Grid.Column="0" Margin="40,30,40,30" Grid.Row="2" Command="{Binding PlusButtonClose}" CommandParameter="{Binding ElementName=AddWindow}" Content="CANCEL" IsCancel="True" />
        <Button Grid.Column="1" Margin="40,30,40,30" Grid.Row="2" Content="ADD" MinWidth="40" Command="{Binding AddItems}" CommandParameter="{Binding ElementName=AddWindow}"/>
    </Grid>
</Window>
